# Tasks

- [x] Inspect route, sidebar, minapp, launchpad, and artifact library patterns.
- [x] Add a route-ready artifacts page using existing artifact library/designer componentry.
- [x] Register `/artifacts` in the renderer router while keeping settings artifact management available.
- [x] Add `artifacts` sidebar type/default/icon/path/manager/label support.
- [x] Add a narrow sidebar migration/backfill that preserves customized visible/disabled lists.
- [x] Register a route-backed internal artifact mini-app/library entry without changing external webview behavior.
- [x] Add i18n keys for page, sidebar, and mini-app labels.
- [x] Add focused tests for sidebar defaults/labels, migration behavior, route-backed mini-app navigation, and page smoke behavior where practical.
- [x] Run targeted tests and `openspec validate artifact-011-navigation-and-miniapp --strict`.
- [x] Run `pnpm lint`, `pnpm test`, and `pnpm format`.
